import React from 'react';
import './navigation-bar.scss';

const NavigationBar = (): React.ReactElement => {
  return <nav className="navigation-bar-container">Nav</nav>;
};

export default NavigationBar;
